{{- if and .Values.n8n.persistence.enabled (not .Values.n8n.persistence.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "n8n.fullname" . }}
  {{- with .Values.n8n.persistence.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value }}
    {{- end }}
  {{- end }}
  labels:
    {{- include "n8n.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- range .Values.n8n.persistence.accessModes }}
    - {{ . | quote }}
    {{- end }}
  resources:
    requests:
      storage: {{ .Values.n8n.persistence.size }}
  {{- if .Values.n8n.persistence.storageClass }}
  {{- if eq "-" .Values.n8n.persistence.storageClass }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ .Values.n8n.persistence.storageClass }}
  {{- end }}
  {{- end }}
{{- end }}
