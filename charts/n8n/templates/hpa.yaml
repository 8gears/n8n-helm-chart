{{- if .Values.n8n.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "n8n.fullname" . }}
  labels:
    {{- include "n8n.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "n8n.fullname" . }}
  minReplicas: {{ .Values.n8n.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.n8n.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.n8n.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.n8n.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.n8n.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.n8n.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
